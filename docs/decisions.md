# 重要決策

## 使用 Next.js App Router

頁面以 `app` 目錄與路由資料夾建立，讓每個學習模式和科目都有清楚、可直接開啟的網址。

## 模式採用一致版面、不同主題色

姐姐使用粉紅、妹妹使用綠色、家長使用灰色；一致的卡片版面降低理解與維護成本。

## 先以靜態內容驗證學習流程

目前題目與按鈕不包含事件或資料儲存，先確認畫面、內容與導覽是否符合學習情境，再加入互動與紀錄。

## 首頁作為模式入口

首頁集中呈現產品主張與三種使用模式，讓家庭成員能快速選擇適合的入口。

